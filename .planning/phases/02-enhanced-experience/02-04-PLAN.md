---
phase: 02-enhanced-experience
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/App.tsx
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "All new components are visible and functional in the App layout"
    - "Layout is responsive on mobile (320px), tablet (768px), and desktop (1024px+)"
    - "Build passes without errors"
    - "No visual regressions in existing components"
  artifacts:
    - path: "src/App.tsx"
      provides: "Integrated layout with all Phase 2 components"
      exports: ["default"]
      contains: ["SoundSelector", "SubdivisionSelector", "BeatNumberDisplay", "BeatIndicators"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/SoundSelector.tsx"
      via: "import and render"
      pattern: "import.*SoundSelector|SoundSelector /\>"
    - from: "src/App.tsx"
      to: "src/components/SubdivisionSelector.tsx"
      via: "import and render"
      pattern: "import.*SubdivisionSelector|SubdivisionSelector /\>"
    - from: "src/App.tsx"
      to: "src/components/BeatNumberDisplay.tsx"
      via: "import and render"
      pattern: "import.*BeatNumberDisplay|BeatNumberDisplay /\>"
---

<objective>
Integrate all Phase 2 components into the App layout with responsive design verification.

Purpose: Wire all new features together in the main application.
Output: Updated App.tsx with all components placed logically.
</objective>

<execution_context>
@/home/jorge/dev/metronome/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/App.tsx
@src/components/SoundSelector.tsx
@src/components/SubdivisionSelector.tsx
@src/components/BeatIndicators.tsx
@src/components/BeatNumberDisplay.tsx

## Existing App Layout
```tsx
<main role="main" aria-label="Metronome controls" className="min-h-screen flex flex-col items-center gap-6 p-4 md:p-8 max-w-2xl mx-auto bg-white dark:bg-gray-900">
  <h1 className="text-2xl font-bold text-gray-900 dark:text-white">Metrônomo</h1>
  <BpmDisplay bpm={state.bpm} />
  <BeatIndicators />
  <PlayButton />
  <div className="flex flex-wrap justify-center gap-4 w-full">
    <BpmControls />
    <TapTempoButton />
  </div>
  <div className="flex flex-wrap justify-center gap-4 w-full">
    <TimeSignatureSelector />
    <AccentToggle />
    <VolumeControl />
  </div>
</main>
```

## New Components to Integrate
- SoundSelector — sound preset toggle buttons
- SubdivisionSelector — subdivision toggle buttons
- BeatNumberDisplay — current beat / total beats display

## Layout Considerations
- BeatNumberDisplay should be prominent, near the BPM display
- SoundSelector and SubdivisionSelector are controls, should be in control rows
- Must not break existing responsive behavior
- Maintain dark mode support
- Maintain accessibility (proper heading order, landmarks)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate all new components into App.tsx layout</name>
  <files>src/App.tsx</files>
  <action>
Modify src/App.tsx:

1. Add imports for new components:
```typescript
import { SoundSelector } from './components/SoundSelector';
import { SubdivisionSelector } from './components/SubdivisionSelector';
import { BeatNumberDisplay } from './components/BeatNumberDisplay';
```

2. Update the layout to include new components in logical positions:

```tsx
function MetronomeApp() {
  const { state } = useMetronome();

  return (
    <main
      role="main"
      aria-label="Metronome controls"
      className="min-h-screen flex flex-col items-center gap-6 p-4 md:p-8 max-w-2xl mx-auto bg-white dark:bg-gray-900"
    >
      <h1 className="text-2xl font-bold text-gray-900 dark:text-white">Metrônomo</h1>

      <BpmDisplay bpm={state.bpm} />

      <BeatNumberDisplay />

      <BeatIndicators />

      <PlayButton />

      <div className="flex flex-wrap justify-center gap-4 w-full">
        <BpmControls />
        <TapTempoButton />
      </div>

      <div className="flex flex-wrap justify-center gap-4 w-full">
        <TimeSignatureSelector />
        <AccentToggle />
        <VolumeControl />
      </div>

      <div className="flex flex-wrap justify-center gap-4 w-full">
        <SoundSelector />
        <SubdivisionSelector />
      </div>
    </main>
  );
}
```

3. Layout order (top to bottom):
   - Title
   - BPM display (existing)
   - BeatNumberDisplay (NEW — below BPM)
   - BeatIndicators (existing)
   - PlayButton (existing)
   - Control row 1: BPM controls + Tap tempo (existing)
   - Control row 2: Time signature + Accent + Volume (existing)
   - Control row 3: SoundSelector + SubdivisionSelector (NEW)

4. Ensure all existing components remain in the same relative order.

5. The new control row 3 uses the same flex-wrap pattern as existing rows for consistency.

6. Ensure gap-6 spacing between sections is maintained.

DO NOT remove any existing components.
DO NOT change the order of existing components.
DO NOT modify component props unless necessary.
  </action>
  <verify>
    <automated>grep -c "SoundSelector" src/App.tsx && grep -c "SubdivisionSelector" src/App.tsx && grep -c "BeatNumberDisplay" src/App.tsx && grep -c "BpmDisplay" src/App.tsx && grep -c "PlayButton" src/App.tsx && grep -c "BpmControls" src/App.tsx && grep -c "TapTempoButton" src/App.tsx && grep -c "TimeSignatureSelector" src/App.tsx && grep -c "AccentToggle" src/App.tsx && grep -c "VolumeControl" src/App.tsx</automated>
  </verify>
  <done>
    - All 3 new components imported and rendered
    - All 9 existing components still present
    - BeatNumberDisplay is below BpmDisplay
    - SoundSelector and SubdivisionSelector are in a new control row
    - Layout uses same flex-wrap pattern as existing rows
    - gap-6 spacing maintained
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify responsive behavior and accessibility</name>
  <files>src/App.tsx</files>
  <action>
1. Verify responsive design:
   - Check that `flex-wrap` is used on all control rows
   - Check that `gap-4` spacing handles wrapping gracefully
   - Check that `max-w-2xl mx-auto` limits width on large screens
   - Check that `p-4 md:p-8` provides appropriate padding on mobile vs desktop

2. Verify accessibility:
   - `role="main"` and `aria-label="Metronome controls"` still present
   - All interactive components are inside the main landmark
   - Heading structure is logical (only one h1)

3. Verify dark mode:
   - `dark:bg-gray-900` on main container
   - New components should inherit dark mode via Tailwind `dark:` classes
   - Check that text colors use `dark:text-white` / `dark:text-gray-400`

4. Check that BeatNumberDisplay doesn't cause layout shift:
   - `tabular-nums` should prevent number width changes
   - Container should have fixed or min height

5. Verify mobile viewport:
   - All controls should wrap to multiple lines on narrow screens (320px)
   - Touch targets should remain 44px minimum
   - Text should remain readable
  </action>
  <verify>
    <automated>grep -c "flex-wrap" src/App.tsx && grep -c "max-w-2xl" src/App.tsx && grep -c "p-4 md:p-8" src/App.tsx && grep -c "role=\"main\"" src/App.tsx && grep -c "aria-label=\"Metronome controls\"" src/App.tsx</automated>
  </verify>
  <done>
    - All control rows use flex-wrap
    - max-w-2xl mx-auto present
    - p-4 md:p-8 padding present
    - role="main" and aria-label present
    - Only one h1 element
    - Dark mode classes present
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification and final checks</name>
  <files>src/App.tsx</files>
  <action>
1. Run TypeScript type check:
```bash
npx tsc --noEmit
```

2. Run production build:
```bash
npm run build
```

3. Verify no console errors in the build output.

4. Run a quick grep to ensure all Phase 2 requirements are addressed in the code:
```bash
# AUDIO-04: 3 sounds
grep -c "classic.*wood.*click\|wood.*click\|classic.*wood" src/audio/sounds.ts

# AUDIO-05: Different sounds for accented vs unaccented
grep -c "accentFrequency" src/audio/sounds.ts

# UI-01: Visual pulse
grep -c "scale-125" src/components/BeatIndicators.tsx

# UI-02: Beat number
grep -c "BeatNumberDisplay" src/App.tsx

# UI-03: First beat distinguished
grep -c "bg-red-500" src/components/BeatIndicators.tsx

# UI-04: Responsive
grep -c "flex-wrap" src/App.tsx

# SUBD-01 to SUBD-04: Subdivisions
grep -c "quarter\|eighth\|triplet\|sixteenth" src/audio/scheduler.ts

# SUBD-05: Visual clear
grep -c "SubdivisionSelector" src/App.tsx
```

5. Create a quick checklist verification:
- [ ] 3 sound presets exist
- [ ] Accent frequency differs from normal
- [ ] Scale animation present
- [ ] Beat number display integrated
- [ ] Red color for beat 1
- [ ] Responsive layout
- [ ] All 4 subdivisions supported
- [ ] Subdivision UI visible
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - TypeScript compilation passes with zero errors
    - Production build succeeds
    - All Phase 2 components are imported and rendered
    - No build warnings or errors
    - All requirements have corresponding code
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx → Components | Layout must not break component isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | Layout shift | mitigate | tabular-nums prevents number width jitter |
| T-02-09 | Information Disclosure | Component props | accept | No sensitive data in props |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] All new components imported in App.tsx
- [ ] All existing components still present
- [ ] Layout is responsive (flex-wrap on all rows)
- [ ] Accessibility landmarks preserved
</verification>

<success_criteria>
- App.tsx integrates all Phase 2 components without breaking existing layout
- Layout remains responsive on all screen sizes
- Build passes with zero errors
- All Phase 2 requirements are verifiable in the code
- No visual regressions in Phase 1 components
</success_criteria>

<output>
After completion, create `.planning/phases/02-enhanced-experience/02-04-SUMMARY.md`
</output>
