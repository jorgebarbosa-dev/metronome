---
phase: 01-core-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02-PLAN.md
files_modified:
  - src/App.tsx
  - src/components/BpmDisplay.tsx
  - src/components/PlayButton.tsx
  - src/components/BpmControls.tsx
  - src/components/TapTempoButton.tsx
  - src/components/TimeSignatureSelector.tsx
  - src/components/VolumeControl.tsx
  - src/components/AccentToggle.tsx
  - src/components/BeatIndicators.tsx
autonomous: true
requirements:
  - BASIC-01
  - BASIC-02
  - BASIC-03
  - BASIC-04
  - BASIC-06
  - BASIC-07
must_haves:
  truths:
    - "User sees large BPM display in the center of the screen"
    - "User can press a prominent play/pause button to start/stop the metronome"
    - "User can change BPM via numeric input, slider, and +/- buttons"
    - "User can tap a button rhythmically to set BPM via tap tempo"
    - "User can select time signature from dropdown (1/4 to 8/8)"
    - "User can toggle accent on/off"
    - "User can control volume with a slider and mute button"
    - "Beat indicators show current beat position with beat 1 visually distinguished"
    - "All controls have ARIA labels and are keyboard accessible"
    - "Layout works on mobile, tablet, and desktop screens"
  artifacts:
    - path: "src/App.tsx"
      provides: "Main layout with centralized design — BPM center, play below, controls inline"
      min_lines: 40
    - path: "src/components/BpmDisplay.tsx"
      provides: "Large BPM number display with current beat indicator"
      min_lines: 20
    - path: "src/components/PlayButton.tsx"
      provides: "Large prominent play/pause toggle button"
      min_lines: 20
    - path: "src/components/BpmControls.tsx"
      provides: "BPM numeric input, range slider, and +/- step buttons"
      min_lines: 35
    - path: "src/components/TapTempoButton.tsx"
      provides: "Tap tempo button with visual feedback"
      min_lines: 20
    - path: "src/components/TimeSignatureSelector.tsx"
      provides: "Dropdown/time signature selector (1/4, 2/4, 3/4, 4/4, 5/4, 6/8, 7/8, 8/8)"
      min_lines: 20
    - path: "src/components/VolumeControl.tsx"
      provides: "Volume slider and mute toggle"
      min_lines: 25
    - path: "src/components/AccentToggle.tsx"
      provides: "Accent on/off toggle button"
      min_lines: 15
    - path: "src/components/BeatIndicators.tsx"
      provides: "Horizontal row of circles showing beat position, beat 1 in different color"
      min_lines: 25
  key_links:
    - from: "src/App.tsx"
      to: "src/context/MetronomeContext.tsx"
      via: "App wraps all components in MetronomeProvider"
      pattern: "MetronomeProvider"
    - from: "src/components/PlayButton.tsx"
      to: "src/context/MetronomeContext.tsx"
      via: "PlayButton calls dispatch({ type: 'TOGGLE_PLAY' })"
      pattern: "dispatch.*TOGGLE_PLAY"
    - from: "src/components/BpmControls.tsx"
      to: "src/context/MetronomeContext.tsx"
      via: "BpmControls dispatches SET_BPM actions"
      pattern: "dispatch.*SET_BPM"
    - from: "src/components/BeatIndicators.tsx"
      to: "src/context/MetronomeContext.tsx"
      via: "BeatIndicators reads currentBeat and timeSignature from context"
      pattern: "currentBeat\|timeSignature"
---

<objective>
Build the complete user interface for the metronome with centralized layout, responsive design, and full accessibility. All controls are visible by default with no hidden sections.

Purpose: Provide an intuitive, accessible interface that works across all devices.
Output: Fully styled React app with all metronome controls, visual feedback, and keyboard navigation.
</objective>

<execution_context>
@/home/jorge/dev/metronome/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-core-engine/01-02-PLAN.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@/home/jorge/dev/metronome/.opencode/get-shit-done/references/ui-brand.md

## Prior Plan Output (01-02)

Plan 02 creates:
- `src/context/MetronomeContext.tsx` — MetronomeProvider, useMetronome hook
- `src/hooks/useLocalStorage.ts` — useLocalStorage hook
- `src/hooks/useTapTempo.ts` — useTapTempo hook

## Interfaces from Plan 02

From `src/context/MetronomeContext.tsx`:
```typescript
interface MetronomeContextType {
  state: MetronomeState;
  currentBeat: number;
  dispatch: React.Dispatch<Action>;
  audioEngine: AudioEngine;
}

export function MetronomeProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useMetronome(): MetronomeContextType;

// Actions:
// { type: 'SET_BPM'; payload: number }
// { type: 'TOGGLE_PLAY' }
// { type: 'SET_TIME_SIGNATURE'; payload: TimeSignature }
// { type: 'TOGGLE_ACCENT' }
// { type: 'SET_VOLUME'; payload: number }
```

From `src/hooks/useTapTempo.ts`:
```typescript
export function useTapTempo(): {
  bpm: number | null;
  tap: () => void;
  reset: () => void;
  isListening: boolean;
};
```

## Design Decisions (from CONTEXT.md)

- **D-05:** Layout centralizado — BPM grande no centro, play/pause abaixo, controles secundários em linha
- **D-06:** Todos os controles visíveis por padrão
- **D-07:** Sem seção "avançada" oculta
- **D-08:** Indicadores de tempo como pequenos círculos dispostos em linha horizontal
- **D-09:** Beat 1 é visualmente destacado com cor diferente dos outros beats
- **D-10:** Display principal mostra BPM grande + botão Play/Pause integrado no centro

## Accessibility Requirements (from PROJECT.md)

- Keyboard navigation (Tab, Enter, Space, arrow keys)
- ARIA labels for all controls
- Sufficient color contrast
- Don't rely on color alone for information
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create display components and App layout shell</name>
  <files>src/App.tsx, src/components/BpmDisplay.tsx, src/components/PlayButton.tsx, src/components/BeatIndicators.tsx</files>
  <action>
    1. Create `src/components/BpmDisplay.tsx`:
       - Accepts `bpm: number` prop
       - Displays BPM as a large number (text-6xl or larger on desktop, text-5xl on mobile)
       - Shows "BPM" label below the number in smaller text
       - Uses Tailwind classes for styling
       - Has `aria-label={\`BPM: \${bpm}\`}` for screen readers
       - Center-aligned
    
    2. Create `src/components/PlayButton.tsx`:
       - Uses `useMetronome()` to get `state.isPlaying` and `dispatch`
       - Large circular button (w-20 h-20 or larger) with prominent styling
       - Shows Play icon (from lucide-react: `Play`) when stopped, Pause icon (`Pause`) when playing
       - On click: dispatches `{ type: 'TOGGLE_PLAY' }`
       - `aria-label={isPlaying ? 'Pause metronome' : 'Start metronome'}`
       - Keyboard accessible: responds to Enter and Space keys
       - Visual feedback: slightly scale down on active state
       - Color: primary brand color (e.g., bg-blue-600 hover:bg-blue-700) when playing, lighter when paused
    
    3. Create `src/components/BeatIndicators.tsx`:
       - Uses `useMetronome()` to get `currentBeat` and `state.timeSignature`
       - Renders a horizontal flex row of circles
       - Number of circles = `timeSignature.beats`
       - Each circle: w-4 h-4 rounded-full
       - Active beat (matching `currentBeat`): filled with primary color (bg-blue-600)
       - Beat 1 when active: use a distinct color (bg-red-500) per D-09
       - Inactive beats: bg-gray-300 (dark: bg-gray-600)
       - Container has `role="status"` and `aria-label={\`Beat \${currentBeat} of \${timeSignature.beats}\`}`
       - Smooth transition between active states (transition-colors duration-150)
       - Gap between circles: gap-2
    
    4. Create `src/App.tsx` layout:
       - Wrap everything in `MetronomeProvider`
       - Use `min-h-screen`, center content with flex column
       - Structure (top to bottom):
         a. Header/title (optional, small): "Metrônomo"
         b. `BpmDisplay` (large, center)
         c. `BeatIndicators` (below BPM, small)
         d. `PlayButton` (prominent, below indicators)
         e. Control row with: `BpmControls`, `TapTempoButton` (side by side on desktop, stacked on mobile)
         f. Secondary controls row: `TimeSignatureSelector`, `AccentToggle`, `VolumeControl`
       - Use responsive grid/flex: `flex flex-col items-center gap-6 p-4 md:p-8`
       - Control rows: `flex flex-wrap justify-center gap-4`
       - Import all components (they'll be created in Task 2)
       - Add `import './index.css'` at the top
  </action>
  <verify>
    <automated>
      test -f src/App.tsx && \
      test -f src/components/BpmDisplay.tsx && \
      test -f src/components/PlayButton.tsx && \
      test -f src/components/BeatIndicators.tsx && \
      grep -q "MetronomeProvider" src/App.tsx && \
      grep -q "BpmDisplay" src/App.tsx && \
      grep -q "PlayButton" src/App.tsx && \
      grep -q "BeatIndicators" src/App.tsx && \
      grep -q "aria-label" src/components/PlayButton.tsx && \
      grep -q "aria-label" src/components/BeatIndicators.tsx && \
      grep -q "role=\"status\"" src/components/BeatIndicators.tsx && \
      echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - App.tsx wraps all components in MetronomeProvider with responsive layout
    - BpmDisplay shows large centered BPM with aria-label
    - PlayButton is large, circular, toggles play/pause with aria-label
    - BeatIndicators renders horizontal circles with beat 1 distinguished by color
    - All display components have proper accessibility attributes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create control components</name>
  <files>src/components/BpmControls.tsx, src/components/TapTempoButton.tsx, src/components/TimeSignatureSelector.tsx, src/components/VolumeControl.tsx, src/components/AccentToggle.tsx</files>
  <action>
    1. Create `src/components/BpmControls.tsx`:
       - Uses `useMetronome()` for `state.bpm` and `dispatch`
       - Three BPM controls in a flex row:
         a. **Minus button**: `-` decrements BPM by 1, `aria-label="Decrease BPM"`
         b. **Numeric input**: `<input type="number">` showing current BPM, min=40, max=240, on change dispatches SET_BPM with parsed value (clamped to 40-240). `aria-label="BPM value"`
         c. **Plus button**: `+` increments BPM by 1, `aria-label="Increase BPM"`
       - Below the row: **Range slider**: `<input type="range">` min=40, max=240, value={bpm}, on change dispatches SET_BPM. `aria-label="BPM slider"`
       - All controls are keyboard accessible
       - Styled with Tailwind: buttons as small rounded squares, input as bordered field
    
    2. Create `src/components/TapTempoButton.tsx`:
       - Uses `useMetronome()` for `dispatch`
       - Uses `useTapTempo()` for `bpm`, `tap`, `isListening`
       - Button labeled "Tap Tempo" (or "Tapping..." when isListening)
       - On click: calls `tap()`, then if `bpm` is not null, dispatches `{ type: 'SET_BPM', payload: bpm }`
       - Visual feedback: pulse animation or color change when isListening
       - `aria-label={isListening ? 'Tap tempo (listening)' : 'Tap tempo'}`
       - Large enough touch target (min-h-12 min-w-24)
    
    3. Create `src/components/TimeSignatureSelector.tsx`:
       - Uses `useMetronome()` for `state.timeSignature` and `dispatch`
       - Dropdown `<select>` with options: 1/4, 2/4, 3/4, 4/4, 5/4, 6/8, 7/8, 8/8
       - On change: parse "beats/beatValue" from value, dispatch `{ type: 'SET_TIME_SIGNATURE', payload: { beats, beatValue } }`
       - `aria-label="Time signature"`
       - Styled with Tailwind: bordered select, rounded
    
    4. Create `src/components/VolumeControl.tsx`:
       - Uses `useMetronome()` for `state.volume` and `dispatch`
       - Horizontal flex row:
         a. **Mute button**: Speaker icon (lucide-react: `Volume2` when unmuted, `VolumeX` when muted/volume=0). On click: if volume > 0, dispatch SET_VOLUME with 0; else dispatch SET_VOLUME with 0.8 (or last non-zero). `aria-label={volume > 0 ? 'Mute' : 'Unmute'}`
         b. **Volume slider**: `<input type="range">` min=0, max=1, step=0.01, value={volume}, on change dispatches SET_VOLUME. `aria-label="Volume"`
       - Show volume percentage text next to slider (e.g., "80%")
       - Styled with Tailwind
    
    5. Create `src/components/AccentToggle.tsx`:
       - Uses `useMetronome()` for `state.accentEnabled` and `dispatch`
       - Toggle button showing "Accent: On" / "Accent: Off"
       - On click: dispatches `{ type: 'TOGGLE_ACCENT' }`
       - Visual state: different background color when enabled vs disabled
       - `aria-pressed={accentEnabled}`
       - `aria-label={accentEnabled ? 'Disable accent' : 'Enable accent'}`
       - Use `ToggleLeft` / `ToggleRight` icons from lucide-react or simple text
  </action>
  <verify>
    <automated>
      test -f src/components/BpmControls.tsx && \
      test -f src/components/TapTempoButton.tsx && \
      test -f src/components/TimeSignatureSelector.tsx && \
      test -f src/components/VolumeControl.tsx && \
      test -f src/components/AccentToggle.tsx && \
      grep -q "SET_BPM" src/components/BpmControls.tsx && \
      grep -q "type=\"number\"" src/components/BpmControls.tsx && \
      grep -q "type=\"range\"" src/components/BpmControls.tsx && \
      grep -q "useTapTempo" src/components/TapTempoButton.tsx && \
      grep -q "tap" src/components/TapTempoButton.tsx && \
      grep -q "SET_TIME_SIGNATURE" src/components/TimeSignatureSelector.tsx && \
      grep -q "SET_VOLUME" src/components/VolumeControl.tsx && \
      grep -q "TOGGLE_ACCENT" src/components/AccentToggle.tsx && \
      grep -q "aria-label" src/components/BpmControls.tsx && \
      grep -q "aria-label" src/components/VolumeControl.tsx && \
      echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - BpmControls has numeric input, +/- buttons, and range slider for BPM (40-240)
    - TapTempoButton integrates useTapTempo and dispatches SET_BPM
    - TimeSignatureSelector has dropdown with all 8 time signatures
    - VolumeControl has mute button and volume slider
    - AccentToggle toggles accent on/off with aria-pressed
    - All controls dispatch correct actions and have aria-labels
  </done>
</task>

<task type="auto">
  <name>Task 3: Final integration, accessibility polish, and build verification</name>
  <files>src/App.tsx, src/main.tsx</files>
  <action>
    1. Update `src/main.tsx` to ensure it renders App correctly:
       ```tsx
       import React from 'react';
       import ReactDOM from 'react-dom/client';
       import App from './App';
       import './index.css';

       ReactDOM.createRoot(document.getElementById('root')!).render(
         <React.StrictMode>
           <App />
         </React.StrictMode>
       );
       ```
    
    2. Update `src/App.tsx` to:
       - Ensure all component imports are correct
       - Add a `<main>` landmark element with `role="main"` and `aria-label="Metronome controls"`
       - Verify the layout matches D-05 (centralized):
         * Top: Title (text-2xl font-bold text-center)
         * Center: BpmDisplay (very large)
         * Below: BeatIndicators
         * Below: PlayButton (large, prominent)
         * Below: Row 1 — BpmControls + TapTempoButton (flex, gap-4, wrap)
         * Below: Row 2 — TimeSignatureSelector + AccentToggle + VolumeControl (flex, gap-4, wrap)
       - Add responsive padding: `p-4 md:p-8 max-w-2xl mx-auto`
       - Ensure sufficient color contrast (dark text on light background or vice versa)
    
    3. Accessibility audit checklist:
       - All interactive elements have `aria-label` or visible text
       - PlayButton is focusable and responds to Enter/Space
       - Numeric inputs have min/max attributes
       - Range inputs have aria-label
       - BeatIndicators has `role="status"` for screen reader announcements
       - Color is not the only indicator (BeatIndicators use filled vs hollow, not just color)
       - Tap target sizes are at least 44x44px for mobile
    
    4. Verify build:
       ```bash
       npm run build
       ```
       Must complete without errors.
    
    5. Verify TypeScript:
       ```bash
       npx tsc --noEmit
       ```
       Must pass without errors.
    
    6. Check that index.html has proper meta tags for mobile:
       - `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
       - `<meta name="description" content="Precise web metronome">`
  </action>
  <verify>
    <automated>
      npm run build 2>&1 >/dev/null && \
      npx tsc --noEmit 2>&1 >/dev/null && \
      grep -q 'meta name="viewport"' index.html && \
      grep -q "role=\"main\"" src/App.tsx && \
      grep -q "aria-label" src/App.tsx && \
      grep -q "BpmControls" src/App.tsx && \
      grep -q "TapTempoButton" src/App.tsx && \
      grep -q "TimeSignatureSelector" src/App.tsx && \
      grep -q "VolumeControl" src/App.tsx && \
      grep -q "AccentToggle" src/App.tsx && \
      echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - App.tsx integrates all components with responsive centralized layout
    - All components are imported and rendered
    - Build succeeds with no errors
    - TypeScript compiles cleanly
    - index.html has proper viewport meta tag
    - Accessibility: aria-labels, roles, keyboard navigation all present
    - Layout works on mobile (tested via responsive classes)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → State | All user inputs are bounded (BPM 40-240, volume 0-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Fake beat indicators | accept | UI is client-side only; no security boundary crossed |
| T-01-05 | Tampering | BPM manipulation via DOM | accept | All state changes go through reducer; no direct state mutation from DOM |
| T-01-06 | Denial of Service | Rapid UI interaction | mitigate | Input min/max constraints, React batching, scheduler throttling |
</threat_model>

<verification>
## Phase 1 Plan 3 Verification

1. `npm run build` succeeds
2. `npx tsc --noEmit` passes
3. App renders without React errors
4. All 9 components are present and imported
5. Play button is large and prominent
6. BPM display is large and centered
7. All controls have aria-label attributes
8. Beat indicators show correct number of circles matching time signature
9. Layout is responsive (check with browser devtools mobile view)
</verification>

<success_criteria>
- Complete UI with all controls: play/pause, BPM (numeric/slider/±), tap tempo, time signature, accent toggle, volume
- Centralized layout per D-05: BPM large in center, play below, controls in rows
- All controls visible by default per D-06, no hidden sections per D-07
- Beat indicators as horizontal circles per D-08, beat 1 distinguished per D-09
- Large BPM + play integrated per D-10
- Full accessibility: ARIA labels, keyboard navigation, color contrast
- Responsive: works on mobile, tablet, desktop
- Build and TypeScript compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-03-SUMMARY.md`
</output>
