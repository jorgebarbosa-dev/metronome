---
phase: 07-native-mobile-ui
plan: 06
type: execute
wave: 5
depends_on:
  - 07-04
  - 07-05
files_modified:
  - src/components/FullscreenView.tsx
  - src/hooks/useKeyboardShortcuts.ts
autonomous: true
requirements:
  - MOB-02
  - MOB-06
  - MOB-07
  - MOB-08

must_haves:
  truths:
    - "FullscreenView uses dark neutral-950 background and matches native mobile styling"
    - "FullscreenView has large beat indicator with pulse ring and glow shadow"
    - "FullscreenView displays BPM in gradient text with tabular nums"
    - "Keyboard shortcuts work with new layout components"
    - "All 8 success criteria from ROADMAP are verifiable"
    - "No hover-dependent interactions without @media(hover:hover) guard"
    - "All touch targets meet minimum 56px (secondary) / 88px (primary)"
  artifacts:
    - path: "src/components/FullscreenView.tsx"
      provides: "Updated fullscreen view with native mobile styling"
      min_lines: 120
    - path: "src/hooks/useKeyboardShortcuts.ts"
      provides: "Updated keyboard shortcuts for new layout"
      min_lines: 40
  key_links:
    - from: "src/hooks/useKeyboardShortcuts.ts"
      to: "src/components/PlayerView.tsx"
      via: "keyboard events control play/pause and BPM"
      pattern: "TOGGLE_PLAY|SET_BPM"
    - from: "src/components/FullscreenView.tsx"
      to: "src/components/BeatIndicator.tsx"
      via: "can optionally reuse BeatIndicator component"
      pattern: "BeatIndicator"
---

<objective>
Update the fullscreen view to match the native mobile dark styling and ensure keyboard shortcuts work with the new layout. Perform final verification against all 8 success criteria.

Purpose: The fullscreen view is a critical user-facing feature from Phase 6. It must visually align with the new native mobile design language while preserving all existing functionality.
Output: Updated FullscreenView.tsx, updated useKeyboardShortcuts.ts, and verified success criteria.
</objective>

<execution_context>
@/home/jorge/dev/metronome/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-native-mobile-ui/07-CONTEXT.md
@/home/jorge/dev/metronome/src/components/FullscreenView.tsx
@/home/jorge/dev/metronome/src/hooks/useKeyboardShortcuts.ts
@/home/jorge/dev/metronome/src/components/BeatIndicator.tsx
@/home/jorge/dev/metronome/ROADMAP.md
</context>

<interfaces>
<!-- Key components and hooks -->

From src/components/BeatIndicator.tsx (Plan 03):
```typescript
export function BeatIndicator(): JSX.Element;
// Large circle (200px+), pulse ring, beat number, red/blue colors
```

From src/context/MetronomeContext.tsx:
```typescript
export function useMetronome(): MetronomeContextType;
interface MetronomeContextType {
  state: MetronomeState;
  currentBeat: number;
  dispatch: React.Dispatch<Action>;
  trainingConfig: TrainingConfig;
  trainingSession: TrainingSessionState;
}
```

From src/types/training.ts:
```typescript
export interface TrainingSessionState {
  isActive: boolean;
  currentBar: number;
  phase: 'count-in' | 'playing';
  countInBarsRemaining: number;
  silenceIsMuted: boolean;
  silenceBarsRemaining: number;
  autoBpmCurrentValue: number;
  autoBpmBarsUntilNext: number;
  elapsedTime: number;
}
```

CSS utilities available:
```css
.spring-bounce { transition-timing-function: cubic-bezier(0.34, 1.56, 0.64, 1); }
.animate-pulse-ring { animation: pulse-ring 0.6s ease-out forwards; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update FullscreenView with native mobile styling</name>
  <files>src/components/FullscreenView.tsx</files>
  <action>
    Rewrite `src/components/FullscreenView.tsx` to match the native mobile dark styling. The fullscreen view should reuse the BeatIndicator component from Plan 03 when possible, but maintain its own larger sizing for distance viewing.

    ```typescript
    import { Play, Pause, Minimize, Clock } from 'lucide-react';
    import { useMetronome } from '../context/MetronomeContext';
    import { useFullscreen } from '../hooks/useFullscreen';

    export function FullscreenView() {
      const { state, currentBeat, trainingConfig, trainingSession, dispatch } =
        useMetronome();
      const { toggleFullscreen } = useFullscreen();

      const isPlaying = state.isPlaying;
      const isBeatOne = currentBeat === 1;
      const isBeatActive = currentBeat > 0;
      const timeSignatureText = `${state.timeSignature.beats}/${state.timeSignature.beatValue}`;
      const hasTrainingModes =
        trainingConfig.autoBpm.enabled ||
        trainingConfig.silence.enabled ||
        trainingConfig.countIn.enabled;
      const isTrainingActive = trainingSession.isActive;

      return (
        <div
          role="main"
          aria-label="Fullscreen metronome view"
          className="h-[100dvh] w-[100dvw] flex flex-col items-center justify-center bg-neutral-950 text-white p-4 md:p-8 gap-6 md:gap-8 select-none overflow-hidden"
        >
          {/* ARIA live region */}
          <div className="sr-only" role="status" aria-live="polite" aria-atomic="true">
            {isPlaying
              ? `BPM ${state.bpm}, beat ${currentBeat} of ${state.timeSignature.beats}`
              : 'Metronome stopped'}
          </div>

          {/* Top section — Time signature and mode badges */}
          <div className="flex flex-col items-center gap-3">
            <div className="text-3xl md:text-4xl font-bold tabular-nums text-white">
              {timeSignatureText}
            </div>

            {hasTrainingModes && (
              <div className="flex items-center gap-2">
                {trainingConfig.autoBpm.enabled && (
                  <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium border border-blue-400/30 text-blue-400">
                    Auto-BPM
                  </span>
                )}
                {trainingConfig.countIn.enabled && (
                  <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium border border-green-400/30 text-green-400">
                    Count-In
                  </span>
                )}
                {trainingConfig.silence.enabled && (
                  <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium border border-amber-400/30 text-amber-400">
                    Silence
                  </span>
                )}
              </div>
            )}
          </div>

          {/* Center section — Large beat indicator with pulse ring */}
          <div className="relative flex items-center justify-center">
            {/* Pulse ring */}
            {isBeatActive && (
              <div
                key={`fs-pulse-${currentBeat}`}
                className={`
                  absolute w-[340px] h-[340px] md:w-[420px] md:h-[420px] lg:w-[500px] lg:h-[500px]
                  rounded-full border-2 pointer-events-none
                  ${isBeatOne ? 'border-red-500/30' : 'border-blue-500/30'}
                  animate-pulse-ring
                `}
                aria-hidden="true"
              />
            )}

            {/* Beat circle */}
            <div
              className={`
                w-[280px] h-[280px] md:w-[360px] md:h-[360px] lg:w-[440px] lg:h-[440px]
                rounded-full flex items-center justify-center
                transition-all duration-150 spring-bounce
                ${isBeatActive
                  ? isBeatOne
                    ? 'bg-red-600 scale-110 shadow-[0_0_80px_rgba(220,38,38,0.25)]'
                    : 'bg-blue-600 scale-110 shadow-[0_0_80px_rgba(59,130,246,0.25)]'
                  : 'bg-neutral-800 scale-100'
                }
              `}
              aria-hidden="true"
            >
              <span className="text-8xl md:text-9xl font-bold text-white tabular-nums">
                {isPlaying ? currentBeat : '—'}
              </span>
            </div>
          </div>

          {/* BPM display */}
          <div className="flex flex-col items-center gap-1">
            <div className="text-7xl md:text-9xl font-bold tabular-nums bg-gradient-to-b from-white to-neutral-400 bg-clip-text text-transparent">
              {state.bpm}
            </div>
            <div className="text-xl md:text-2xl text-white/30 font-medium uppercase tracking-[0.2em]">BPM</div>
          </div>

          {/* Bottom section — Controls */}
          <div className="flex items-center gap-4 md:gap-6">
            <button
              onClick={() => dispatch({ type: 'TOGGLE_PLAY' })}
              aria-label={isPlaying ? 'Pause metronome' : 'Start metronome'}
              aria-pressed={isPlaying}
              className={`
                w-[88px] h-[88px] md:w-32 md:h-32 rounded-full
                flex items-center justify-center
                transition-transform duration-150 active:scale-[0.88] spring-bounce
                focus:outline-none focus-visible:ring-4 focus-visible:ring-blue-500/50
                ${isPlaying
                  ? 'bg-red-500 shadow-[0_8px_32px_rgba(239,68,68,0.35)]'
                  : 'bg-blue-500 shadow-[0_8px_32px_rgba(59,130,246,0.35)]'
                }
                [@media(hover:hover)]:hover:brightness-110
              `}
            >
              {isPlaying ? (
                <Pause className="w-8 h-8 md:w-12 md:h-12 text-white" aria-hidden="true" />
              ) : (
                <Play className="w-8 h-8 md:w-12 md:h-12 text-white ml-0.5" aria-hidden="true" />
              )}
            </button>

            <button
              onClick={toggleFullscreen}
              aria-label="Exit fullscreen"
              className="w-14 h-14 md:w-20 md:h-20 rounded-full flex items-center justify-center bg-white/[0.06] text-white/70 active:scale-90 transition-transform [@media(hover:hover)]:hover:bg-white/[0.10]"
            >
              <Minimize className="w-6 h-6 md:w-8 md:h-8" aria-hidden="true" />
            </button>
          </div>

          {/* Training overlay */}
          {isTrainingActive && (
            <div className="absolute top-4 left-4 bg-neutral-900/90 backdrop-blur-sm rounded-2xl p-4 border border-white/[0.08]">
              <div className="flex flex-col gap-2">
                <div className="font-mono text-xl text-white/70 flex items-center gap-2">
                  <Clock className="w-4 h-4" aria-hidden="true" />
                  <span className="text-white">
                    {Math.floor(trainingSession.elapsedTime / 60)}:{String(Math.floor(trainingSession.elapsedTime % 60)).padStart(2, '0')}
                  </span>
                </div>

                <div className="font-mono text-2xl text-white">
                  Bar: {trainingSession.currentBar}
                </div>

                <span
                  className={`inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium w-fit ${
                    trainingSession.phase === 'count-in'
                      ? 'bg-yellow-500/20 text-yellow-300 border border-yellow-500/30'
                      : 'bg-green-500/20 text-green-300 border border-green-500/30'
                  }`}
                >
                  {trainingSession.phase === 'count-in' ? 'Count-in' : 'Playing'}
                </span>

                {trainingConfig.autoBpm.enabled && (
                  <div className="text-sm text-white/70">
                    <span className="text-white/40">BPM: </span>
                    <span className="text-white font-mono">{trainingSession.autoBpmCurrentValue}</span>
                    <span className="text-white/40">{' '}{trainingConfig.autoBpm.direction === 'decrease' ? '←' : '→'}{' '}</span>
                    <span className="text-white font-mono">{trainingConfig.autoBpm.targetBpm}</span>
                  </div>
                )}

                {trainingConfig.silence.enabled && (
                  <span
                    className={`inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium w-fit ${
                      trainingSession.silenceIsMuted
                        ? 'bg-red-500/20 text-red-300 border border-red-500/30'
                        : 'bg-green-500/20 text-green-300 border border-green-500/30'
                    }`}
                  >
                    {trainingSession.silenceIsMuted ? 'Silent' : 'Audible'}
                  </span>
                )}

                {trainingConfig.countIn.enabled &&
                  trainingSession.phase === 'count-in' && (
                  <div className="text-sm text-white/70">
                    <span className="text-white/40">Count-in: </span>
                    <span className="text-white font-mono">{trainingSession.countInBarsRemaining}</span>
                    <span className="text-white/40"> bars left</span>
                  </div>
                )}
              </div>
            </div>
          )}
        </div>
      );
    }
    ```

    Key changes:
    - Background: bg-neutral-950 (matches PlayerView)
    - Full viewport: h-[100dvh] w-[100dvw]
    - Beat indicator: larger sizes (280px/360px/440px) with pulse ring and spring-bounce
    - Play button: 88px with spring active:scale-[0.88], red/blue glow
    - BPM: gradient text (bg-gradient-to-b from-white to-neutral-400)
    - Exit fullscreen button: 56px minimum (w-14 h-14)
    - Training overlay: updated to dark theme with border-white/[0.08]
    - Hover guarded by @media(hover:hover)
  </action>
  <verify>
    <automated>grep -q "bg-neutral-950" src/components/FullscreenView.tsx && grep -q "h-\[100dvh\] w-\[100dvw\]" src/components/FullscreenView.tsx && grep -q "spring-bounce" src/components/FullscreenView.tsx && grep -q "bg-gradient-to-b from-white to-neutral-400" src/components/FullscreenView.tsx && grep -q "w-\[88px\] h-\[88px\]" src/components/FullscreenView.tsx && grep -q "\[@media(hover:hover)\]" src/components/FullscreenView.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>FullscreenView.tsx has neutral-950 background, 100dvh/100dvw, larger beat indicator with pulse ring and spring-bounce, gradient BPM text, 88px play button with glow, hover guards, and dark-themed training overlay</done>
</task>

<task type="auto">
  <name>Task 2: Update keyboard shortcuts for new layout</name>
  <files>src/hooks/useKeyboardShortcuts.ts</files>
  <action>
    Read `src/hooks/useKeyboardShortcuts.ts` first, then ensure it works correctly with the new layout. The keyboard shortcuts should continue to work as before (Space for play/pause, arrow keys for BPM, etc.).

    If the file already handles these correctly, no changes may be needed. However, verify:
    1. Space/Enter toggles play/pause via TOGGLE_PLAY
    2. Arrow up/down adjusts BPM by ±1 via SET_BPM
    3. No keyboard shortcuts are broken by the new component structure

    If changes are needed, preserve all existing shortcut behaviors. The hook dispatches to MetronomeContext, which hasn't changed.
  </action>
  <verify>
    <automated>grep -q "TOGGLE_PLAY" src/hooks/useKeyboardShortcuts.ts && grep -q "SET_BPM" src/hooks/useKeyboardShortcuts.ts && grep -q "useMetronome" src/hooks/useKeyboardShortcuts.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Keyboard shortcuts hook still dispatches TOGGLE_PLAY and SET_BPM via useMetronome; all shortcuts from previous phases continue to work</done>
</task>

<task type="auto">
  <name>Task 3: Final verification against all 8 success criteria</name>
  <files>N/A (verification task)</files>
  <action>
    Verify all 8 success criteria from the ROADMAP for Phase 7. For each criterion, run a grep-based check:

    1. **Full-screen edge-to-edge**: `grep -q "h-\[100dvh\] w-\[100dvw\]" src/components/PlayerView.tsx && grep -q "h-\[100dvh\] w-\[100dvw\]" src/components/FullscreenView.tsx`
    2. **Dark mode**: `grep -q "bg-neutral-950" src/components/PlayerView.tsx && grep -q "document.documentElement.classList.add('dark')" src/main.tsx`
    3. **Touch targets 56px/88px**: `grep -q "h-14 min-w-\[56px\]" src/components/ui/ControlButton.tsx && grep -q "w-\[88px\] h-\[88px\]" src/components/PlayButton.tsx`
    4. **Bottom-oriented controls**: `grep -q "flex justify-center" src/components/PlayerView.tsx` (play button centered) && `grep -q "pb-\[calc(20px+env(safe-area-inset-bottom))\]" src/components/PlayerView.tsx`
    5. **Swipe gestures**: `grep -q "useSwipeBpm" src/components/BpmDisplay.tsx`
    6. **Native animations**: `grep -q "spring-bounce" src/components/PlayButton.tsx && grep -q "animate-pulse-ring" src/components/BeatIndicator.tsx`
    7. **Safe areas**: `grep -q "env(safe-area-inset-top)" src/components/PlayerView.tsx && grep -q "env(safe-area-inset-bottom)" src/components/PlayerView.tsx && grep -q "env(safe-area-inset-bottom)" src/components/ui/BottomSheet.tsx`
    8. **No hover on touch**: `grep -r "\[@media(hover:hover)\]" src/components/ --include="*.tsx" | wc -l` should be >= 5

    Run `npm run build` and confirm zero TypeScript errors.

    If any check fails, report which criterion failed and what needs to be fixed.
  </action>
  <verify>
    <automated>
    npm run build 2>&1 && \
    grep -q "h-\[100dvh\] w-\[100dvw\]" src/components/PlayerView.tsx && \
    grep -q "bg-neutral-950" src/components/PlayerView.tsx && \
    grep -q "w-\[88px\] h-\[88px\]" src/components/PlayButton.tsx && \
    grep -q "useSwipeBpm" src/components/BpmDisplay.tsx && \
    grep -q "spring-bounce" src/components/PlayButton.tsx && \
    grep -q "env(safe-area-inset-top)" src/components/PlayerView.tsx && \
    grep -r "\[@media(hover:hover)\]" src/components/ --include="*.tsx" | wc -l | grep -q "[5-9]\|[1-9][0-9]" && \
    echo "ALL_CRITERIA_PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>All 8 success criteria verified via grep checks; npm run build passes with zero TypeScript errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → FullscreenView | Fullscreen is user-triggered; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Denial of Service | FullscreenView pulse ring | mitigate | Same remount pattern as BeatIndicator; bounded by beat scheduler |
| T-07-06-02 | Information Disclosure | Training overlay | accept | Data comes from existing trainingSession state |
| T-07-06-03 | Spoofing | Keyboard shortcuts | accept | Shortcuts dispatch to existing validated reducer |
</threat_model>

<verification>
1. Run `npm run build` and confirm no TypeScript errors
2. Confirm FullscreenView uses neutral-950, 100dvh/100dvw, spring-bounce, gradient BPM
3. Verify all 8 success criteria pass via automated checks
</verification>

<success_criteria>
- FullscreenView.tsx matches native mobile dark styling (neutral-950, gradient text, pulse ring, spring animations)
- FullscreenView play button is 88px with glow shadow and spring active state
- Keyboard shortcuts hook continues to work with new layout
- All 8 ROADMAP success criteria are verifiable via automated checks
- Build passes with zero TypeScript errors
- No hover styles without @media(hover:hover) guard
</success_criteria>

<output>
After completion, create `.planning/phases/07-native-mobile-ui/07-06-SUMMARY.md`
</output>
